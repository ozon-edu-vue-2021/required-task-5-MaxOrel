export  default  {    SET_PRODUCTS_TO_STATE: (state, payload) => {        state.productData = payload.map(function (item) {            return {...item, quantity: 1, favorites: false};        })    },    SET_CART: (state, payload) => {        let isProductExist = false        state.cart.map(function (item) {            if (item.id === payload.id) {                isProductExist = true                item.quantity = payload.quantity            }        })        isProductExist || state.cart.push(payload)    },    SET_FAVORITES: (state, payload) => {        payload.favorites = !payload.favorites;    },    REMOVE_FROM_CART: (state, index) => {        state.cart.splice(index, 1)    },    INCREMENT_CART: (state, productId) => {        state.cart.map(function (item) {            if (item.id === productId) {                item.quantity++            }        })    },    DECREMENT_CART: (state, productId) => {        state.cart.map(function (item) {            if (item.id === productId) {                item.quantity > 1 && item.quantity--            }        })    },    INCREMENT_PRODUCT: (state, productId) => {        state.productData.map(function (item) {            if (item.id === productId) {                item.quantity++            }        })    },    DECREMENT_PRODUCT: (state, productId) => {        state.productData.map(function (item) {            if (item.id === productId) {                item.quantity > 1 && item.quantity--            }        })    }}